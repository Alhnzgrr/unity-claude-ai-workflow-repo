# Skills Library

## core/ (Always Loaded)

- `model-routing.md`: model tier and task routing guidance
- `unity-instincts/`: project-wide quick decisions
- `unity-mcp-patterns/`: Unity MCP usage and fallback patterns
- `context-management/`: review mode, checkpointing, and context economy

## unity-architecture/ (Architecture and Boundaries)

- `unity-clean-architecture/`: testable Unity architecture with thin MonoBehaviours
- `environment-view-separation/`: separation between rules/state and presentation
- `simulation-loop/`: reset, observe, validate, step, events, and completion flow

## unity-core/ (Unity Safety and Runtime Fundamentals)

- `serialization-safety/`: serialized field evolution, `FormerlySerializedAs`, runtime state separation
- `input-system/`: input adapters, action APIs, replay and AI compatibility
- `object-pooling/`: pooled object lifecycle, reset rules, mobile allocation safety
- `editor-runtime-separation/`: safe separation of `UnityEditor` code from runtime code
- `mobile-development/`: mobile CPU, memory, rendering, UI, touch, and thermal guidance

## systems/ (Loaded on Demand)

- `audio/`
- `physics/`
- `animation/`
- `ui-toolkit/`
- `urp-pipeline/`
- `cinemachine/`
- `shader-graph/`
- `addressables/` when `project-config.json` has `"addressables": true`
- `vr/` when `project-config.json` has `"xr": true`

## third-party/ (Loaded Based on Package Detection)

- `vcontainer/`
- `zenject/`
- `unitask/`
- `dotween/`
- `textmeshpro/`

## learned/ (Generated by /learn)

- Project-specific discovered patterns.

## Skill Quality Standard

New and upgraded skills should include:

- Purpose
- Core Idea
- Use When, when routing is not obvious
- Good Pattern and Bad Pattern, when applicable
- Implementation Guidance
- Common Mistakes
- AI Review Guidance
